const _CONFIG = {
    /**
     * @type REQUIRED
     * @description For app title
     */
    app_title: "Master U",

    /**
     * @type REQUIRED
     * @description colors for app
     */
    app_colors: {
        login: {
            background: "#ff9400",
            color: "#fff"
        },
        header: {
            background: "linear-gradient(to right, #06A099 0%, #31AC69 100%)",
            color: "#fff"
        }
    },

       /**
     * @type REQUIRED
     * @description For programs details
     */
        formation_type: ["TÉCNICO", "TECNOLOGO", "PROFESIONAL", "ESPECIALIZACIÓN"],
    /**
     * @type REQUIRED
     * @description For browser storage
     */
    storage_prefix: "masteru",
    /**
     * @type REQUIRED
     */
    api_base_url: 'https://apiexample/api',

    /**
     * @type OPTIONAL
     */
    error_reporting_url: "https://apiexample/errorreporting",

    /**
     * @type OPTIONAL
     * @description This object have to contain the gsi configuration
     */
    google_client: {
        active: true,
        method_name: "google",
        id: 'CLIENT_ID.apps.googleusercontent.com',
        show_one_tap: true,
    },

    /**
     * @type OPTIONAL
     * @description Used if you want check if your app is in maintenance (Have to return a link to redirect if the app is in maintenance)
     */
    maintenance_url: "https://apiexample/maintenance/activau",
}

/*      
    window.onload = () => {
        // Add Analitycs or another function when document is loaded

        const script1 = document.createElement("script");
        script1.type = "text/javascript";
        script1.src = "https://www.googletagmanager.com/gtag/js?id=EXAMPLE";
        document.body.appendChild(script1)
        window.dataLayer = window.dataLayer || [];
        function gtag() { window.dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', 'EXAMPLE');
    }
*/

window._NGconfig = _CONFIG;